<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ajmalsyed.pets_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Catalog_Activity"
            android:label="@string/pets"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Editor_Activity"
            android:label="Edit Pets"
            android:parentActivityName=".Catalog_Activity"></activity>

        <activity android:name=".SearchActivity"
            android:label="Search Pets"
            android:parentActivityName=".Catalog_Activity"></activity>
        <activity
            android:name=".Add_Activity"
            android:label="Add Pets"
            android:parentActivityName=".Catalog_Activity"></activity>
    </application>

</manifest>